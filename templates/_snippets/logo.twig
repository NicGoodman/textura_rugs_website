<a href="{{ siteUrl }}">
	<h1 class="text-rug-white">
		{% if logo|length %}
			<img src="{{ logo.src() }}" alt="{{ logoAltText }}" data-srcset="{{ logo.srcset() }}" class="h-12 lg:h-16 lg:pb-1">
		{% else %}
			{{ siteName }}
		{% endif %}
	</h1>
</a>
