{% set list = craft.wishlist.lists().default(true).one() %}
	<a href="" hx-get="/wish-list" hx-trigger="click" hx-target="#wish-list" class="wish-list-link font-quicksand hover:text-rug-light-blue" @click="showRug = !showRug" :aria-expanded="showRug ? 'true' : 'false'" :class="{ 'active': showRug }">
		Wishlist <i class="fas fa-clipboard-list"></i>
	</a>

