<article id="" class="flex flex-col flex-wrap w-full md:flex-row section-wrapper md:mt-48">
	<section id="" class="">
		<h1>{{ category.collectionName}}</h1>
		<h2>{{ category.collectionDescription }}</h2>
	</section>
	<section id="" class="">
    {% set inventoryQuery = craft.entries().section('inventory').relatedTo(category) %}
		{% include "_pages/inventory-feed-section" %}
	</section>
</article>
