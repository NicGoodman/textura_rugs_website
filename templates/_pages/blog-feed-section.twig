<section id="blog-feed-section" class="flex flex-col flex-wrap section-wrapper">
	<h1 class="">Recent Posts</h1>
	<div id="" class="flex flex-row flex-wrap">
		{% set posts = craft.entries().section('blogPosts') %}
		{% for post in posts.all() %}
			{% include "_content/blog-post-block" %}
		{% endfor %}
	</div>
</section>
