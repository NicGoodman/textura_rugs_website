<article id="" class="w-full p-4 md:w-1/2">
{% set thumbnail = post.thumbnail.one().optimizedImages %}
	<a href="{{ post.getUrl }}" class="flex flex-row">
		<picture class="flex-shrink-0 pr-2">
			<img src="{{ thumbnail.src() }}" alt="" data-srcset="{{ thumbnail.srcset() }}" class="object-cover w-48 h-48">
		</picture>
		<section id="" class="flex flex-col">
			<h2>{{ post.title }}
				•
				{{ post.date|date('long') }}</h2>
			<div>
				{{ post.intro }}
			</div>
		</section>
	</a>
</article>
