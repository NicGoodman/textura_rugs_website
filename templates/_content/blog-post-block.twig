<article id="" class="w-full p-4 lg:w-1/2">
{% set thumbnail = post.thumbnail.one().optimizedImages %}
	<a href="{{ post.getUrl }}" class="flex flex-col md:flex-row">
		<picture class="flex-shrink-0 pt-2 pr-4">
			<img src="{{ thumbnail.src() }}" alt="" data-srcset="{{ thumbnail.srcset() }}" class="object-cover w-full h-48 border-2 md:w-36 border-rug-grey hover:border-rug-light-blue">
		</picture>
		<section id="" class="flex flex-col">
			<h2>{{ post.title }}
				•
				{{ post.date|date('long') }}</h2>
			<div>
				{{ post.intro }}
			</div>
		</section>
	</a>
</article>
