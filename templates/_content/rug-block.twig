<article id="{{ loop.index }}" class="m-4 mix {% if entry.origin|length %}{% for tag in entry.origin.all() %}{{ tag.title|replace(' ', '') }} {% endfor %}{% endif %}{% if entry.age|length %}{% for tag in entry.age.all() %}{{ tag.title|replace(' ', '') }} {% endfor %}{% endif %}{% if entry.primaryColor|length %}{% for tag in entry.primaryColor.all() %}{{ tag.title|replace(' ', '') }} {% endfor %}{% endif %}{% if entry.secondaryColor|length %}{% for tag in entry.secondaryColor.all() %}{{ tag.title|replace(' ', '') }} {% endfor %}{% endif %}{% if entry.collection|length %}{% for category in entry.collection.all() %}{{ category.title|replace(' ', '') }} {% endfor %}{% endif %}{% if entry.sizeCategory|length %}{% for category in entry.sizeCategory.all() %}size{{ category.title|replace(' ', '') }} {% endfor %}{% endif %} w-36 md:w-48 xl:w-56 leading-tight" {# data-origin="{% if entry.origin|length %}{% for tag in entry.origin.all() %} {{ tag.title|replace(' ', '') }}  {% endfor %}{% endif %}" data-age="{% if entry.age|length %}{% for tag in entry.age.all() %} {{ tag.title|replace(' ', '') }}  {% endfor %}{% endif %}" data-primarycolor="{% if entry.primaryColor|length %}{% for tag in entry.primaryColor.all() %} {{ tag.title|replace(' ', '') }}  {% endfor %}{% endif %}" data-secondarycolor="{% if entry.secondaryColor|length %}{% for tag in entry.secondaryColor.all() %} {{ tag.title|replace(' ', '') }}  {% endfor %}{% endif %}" data-collection="{% if entry.collection|length %}{% for category in entry.collection.all() %} {{ category.title|replace(' ', '') }}  {% endfor %}{% endif %}" data-size="{% if entry.size|length %}{% for category in entry.size.all() %} {{ category.title|replace(' ', '') }}  {% endfor %}{% endif %}"#}>
	<a href="{{ entry.getUrl() }}">
		<picture>
			<img src="{{ thumbnail.src() }}" data-srcset="{{ thumbnail.srcset() }}" alt="{{ name }}" class="object-cover border-2 w-36 h-36 md:w-48 md:h-48 xl:w-56 xl:h-56 border-rug-grey hover:border-rug-light-blue">
		</picture>
		<h2 class="mt-2">{{ entry.title }}</h2>
	</a>
</article>
